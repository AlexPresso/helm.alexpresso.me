apiVersion: v1
kind: Service
metadata:
  name: zuninja-svc
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: zuninja-app
  type: ClusterIP
